const DEFAULT_URL = "";
const LOCAL_URL = "";
const BETA_URL = "";
const PROD_URL = "";

export const config = {
	default: {
		enableDevTools: true,
	},
	local: {
		enableDevTools: true,
	},
	beta: {
		enableDevTools: false,
	},
	prod: {
		enableDevTools: false,
	}
};
